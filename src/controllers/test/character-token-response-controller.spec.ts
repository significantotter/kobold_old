describe('character-token-response-controller', () => {
	test('creates an oauth token from a valid response', () => {});
	test("supplies an error when oauth response's state doesn't match a recorded request state", () => {});
	test("supplies an error when an api key can't be fetched from wanderer's guide using the one-time request url", () => {});
});
